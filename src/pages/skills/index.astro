---
import Layout from '../../layouts/Layout.astro';
import { SkillCatalog } from '../../components/skills';
import { getTranslations } from '../../i18n';

const t = getTranslations('ja');
---

<Layout title={t.skillCatalog.title} description={t.skillCatalog.description}>
  <SkillCatalog locale="ja" client:load />
</Layout>
